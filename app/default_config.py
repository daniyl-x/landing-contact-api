CSV_FIELDS = ["date", "email", "name", "message"]
OUTPUT_FILENAME = "contact_us.csv"

# Number of reverse proxies in front of the app.
# Set to 0 in order to run without a reverse proxy.
# Setting this to more than actual proxy count will negatively impact security.
PROXY_COUNT = 0
